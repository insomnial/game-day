# Watches the game specified and reports back to the spawner
#
#
#

import os, requests, json, statsapi
from datetime import datetime, timezone
from dotenv import load_dotenv
from pathlib import Path

#region globals
global GAME_DIR
global CHANNEL_ID
global SLACK_TOKEN
global GAME_ID


def main():
    pass


# I forget this every effing time
if __name__ == '__main__':
    main()
